import React from "react";

const Returns = () => (
  <div className="auth-container">
    <h2>Returns & Exchanges</h2>
    <p>Returns are accepted within 30 days of delivery. Please contact support for exchanges and return instructions.</p>
  </div>
);

export default Returns;
